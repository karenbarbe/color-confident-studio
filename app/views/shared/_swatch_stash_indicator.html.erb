<%# Expects: product_color, is_in_stash %>
<div id="<%= dom_id(product_color, :stash_indicator) %>" class="absolute bottom-1.5 left-1.5 sm:bottom-2 sm:left-2">
  <% if is_in_stash %>
    <div class="size-6 sm:size-7 flex justify-center items-center rounded-full bg-white/85">
      <div class="size-3.5 sm:size-4 flex justify-center items-center text-gray-700">
        <%= icon(:heart_solid) %>
      </div>
    </div>
  <% end %>
</div>
