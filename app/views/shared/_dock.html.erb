<nav class="fixed bottom-0 left-0 right-0 z-10 bg-base-100 border-t border-base-300
            md:top-0 md:bottom-0 md:right-auto md:w-20 md:border-t-0 md:border-r">
  <div class="flex flex-row justify-around items-center h-18
              md:flex-col md:justify-start md:items-stretch md:h-auto md:pt-20 md:gap-1">

    <%= link_to dashboard_path, class: "dock-item group #{current_page?(dashboard_path) ? 'active' : 'text-gray-500'}" do %>
      <span class="dock-indicator"></span>
      <%= icon(:home_outline) %>
      <span class="dock-item-label">Home</span>
    <% end %>

    <%= link_to color_libraries_path, class: "dock-item group #{(controller_name == 'color_libraries' || (controller_name == 'product_colors' && action_name == 'show')) ? 'active' : 'text-gray-500'}" do %>
      <span class="dock-indicator"></span>
      <%= icon(:swatch_outline) %>
      <span class="dock-item-label">Browse</span>
    <% end %>

    <%= link_to new_palette_path, class: "dock-item group #{current_page?(new_palette_path) ? 'active' : 'text-gray-500'}" do %>
      <span class="dock-indicator"></span>
      <%= icon(:create_outline) %>
      <span class="dock-item-label">Create</span>
    <% end %>

    <%= link_to stash_items_path, class: "dock-item group #{controller_name == 'stash_items' ? 'active' : 'text-gray-500'}" do %>
      <span class="dock-indicator"></span>
      <%= icon(:heart_outline) %>
      <span class="dock-item-label">Stash</span>
    <% end %>

    <%= link_to palettes_path, class: "dock-item group #{current_page?(palettes_path) ? 'active' : 'text-gray-500'}" do %>
      <span class="dock-indicator"></span>
      <%= icon(:palette_outline) %>
      <span class="dock-item-label">Palettes</span>
    <% end %>

  </div>
</nav>
